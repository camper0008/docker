# backend

FROM golang:1.19.2

ENV DB_USER=$DB_USER
ENV DB_PASSWORD=$DB_PASSWORD
ENV DB_DATABASE=$DB_DATABASE
ENV DB_HOST=$DB_HOST
ENV PORT=$PORT

COPY src /docker/backend

WORKDIR /docker/backend

RUN go build .
